apiVersion: v1
kind: Service
metadata:
  name: {{ include "inframap.fullname" . }}
  labels:
    {{- include "inframap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "inframap.selectorLabels" . | nindent 4 }}
